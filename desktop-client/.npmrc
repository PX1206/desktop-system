# 勿在此使用 electron_mirror：npm 10+ 会报 Unknown project config，且易被误配成路径导致下载 URL 错乱。
# Electron 下载镜像已写在 package.json -> build.electronDownload.mirror
# 若仍失败，请检查用户目录下 .npmrc / 系统环境变量里是否含有错误的 ELECTRON_MIRROR、electron_mirror（勿指向本地盘符路径）。
